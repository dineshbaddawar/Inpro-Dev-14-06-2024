@istest
public with sharing class QuoteFollowupHelperTest {
    @isTest static void testQuoteFollowups() {
		QuoteFollowupHelper qfuh = new QuoteFollowupHelper();
        qfuh.QuoteFollowupHelper();
        System.assert(true);
	}
}